{
  "name": "intercesso-app",
  "version": "1.0.0",
  "description": "Intercesso Prayer Sharing Platform - Monorepo",
  "scripts": {
    "start": "cd backend && node dist/index.js",
    "build": "cd backend && npm install && npm run build",
    "install:backend": "cd backend && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
